/var/www/*/logs/*.log {
        daily
        missingok
        rotate 7
        compress
        delaycompress
        create
        sharedscripts
        postrotate
                [ ! -f /var/run/nginx.pid ] || kill -USR1 `cat /var/run/nginx.pid`
        endscript
}
